<section id="mostActiveNicks">
	<a name="mostActiveNicks"></a>
	<div class="bar"><h2>Most active nicks</h2></div>

	<table>
		<thead>
			<tr>
				<th>#</th>
				<th>Nick</th>
				<th>Lines</th>
				<th>When</th>
				<th>Words</th>
				<th>Words per line</th>
				<th>Chars per line</th>
				<th>Last seen</th>
				<th>Random quote</th>
			</tr>

		</thead>
		<tbody>
			<% for( var i=0, count=userList.length; i < count; ++i ) { %>

				<tr class="<%= i%2===0?'odd':'even' %>">
					<td class="pos">
						<%= i+1 %>
					</td>
					<td class="nick">
						<%= userList[i].nick %>
					</td>
					<td>
						<%= userList[i].lines %>
					</td>
					<td>
						<%= userList[i].when %>
					</td>
					<td>
						<%= userList[i].words %>
					</td>
					<td>
						<%= userList[i].wordsPerLine %>
					</td>
					<td>
						<%= userList[i].charsPerLine %>
					</td>
					<td class="lastSeen">
						<%= userList[i].lastSeen %>
					</td>
					<td class="quote">
						<%= userList[i].randomQuote %>
					</td>
				</tr>

			<% } %>

		</tbody>

	</table>

	<p>The table should give you an idea of who are the active people, and maybe a bit of insight into their personalities via the random quote.</p>

</section>